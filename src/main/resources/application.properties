# Perfil por defecto para ejecutar el proyecto en local.
# En Railway define SPRING_PROFILES_ACTIVE=prod.
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

spring.application.name=parcial-proyecto-club
server.port=${PORT:8080}
